include kanagawabones.conf

font_family      FiraCode Nerd Font

font_size 12.0
# Font size (in pts).

window_padding_width 5
# The window padding (in pts) 

hide_window_decorations yes
# Hide the window decorations (title-bar and window borders) 


tab_bar_style powerline
# The tab bar style, can be one of:

# fade
#     Each tab's edges fade into the background color. (See also tab_fade)
# slant
#     Tabs look like the tabs in a physical file.
# separator
#     Tabs are separated by a configurable separator. (See also
#     tab_separator)
# powerline
#     Tabs are shown as a continuous line with "fancy" separators.
#     (See also tab_powerline_style)
# custom
#     A user-supplied Python function called draw_tab is loaded from the file
#     tab_bar.py in the kitty config directory. For examples of how to
#     write such a function, see the functions named draw_tab_with_* in
#     kitty's source code: kitty/tab_bar.py. See also
#     this discussion <https://github.com/kovidgoyal/kitty/discussions/4447>
#     for examples from kitty users. hidden
#     The tab bar is hidden. If you use this, you might want to create
#     a mapping for the select_tab action which presents you with a list of
#     tabs and allows for easy switching to a tab.

background_opacity 0.8
# The opacity of the terminal background color. A number between zero
# and one, where one is opaque and zero is fully transparent. 

background_blur 10

# Set to a positive value to enable background blur (blurring of the
# visuals behind a transparent window).
# Only takes effect when background_opacity is less than one. 
# transparent_background_colors
